describe('Full test of the Portuguese Stone Harbor demo', () => {
    it('Checks that Stone Harbor Portuguese works', () => {
        cy.visit('/stone-harbor-pt')
        cy.get('a').contains('roupas').click()
        cy.get('a').contains('anel').click()
        cy.get('a').contains('Nancy?').click()
        cy.get('a').contains('Nadine?').click()
        cy.get('a').contains('Nicky?').click()
        cy.contains('casamento')
        cy.get('a').contains('aparência').click()
        cy.get('a').contains('o escaldão a pelar').click()
        cy.contains('consegues algumas pistas')
        cy.contains('passar tempo ao ar livre')
        cy.contains('anel também ficou esquecido')
        cy.get('a').contains('cartão').click()
        cy.contains('cartão, que viras e lês')
        cy.get('a').contains('luva').click()
        cy.get('a').contains('luva').click()
        cy.get('a').contains('luva').click()
        cy.contains('e tudo muda')
        cy.get('img').should('have.attr', 'alt').and('contains', 'Um escritório pequeno')
        cy.get('a').contains('bibelôs').click()
        cy.get('a').contains('fotografia').click()
        cy.contains('objetos pessoais')
        cy.contains('Examinas a imagem.')
        cy.get('a').contains('luva zangada').click()
        cy.get('a').contains('luva violentamente zangada').click()
        cy.contains('Healey foi assassinado.')
    })
})
